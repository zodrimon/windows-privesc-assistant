# windows-privesc-assistant

A CLI tool that automatically enumerates a Windows system for privilege escalation vectors.

## Installation

**Note:** This tool uses Windows-only APIs and commands. It must be run on a Windows host.

To install:
```bash
pip install .
```
